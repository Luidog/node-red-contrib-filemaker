"use strict";

const {
  compact,
  merge,
  isJson,
  sanitize,
  constructParameters
} = require("./utilities.service");

module.exports = { compact, merge, isJson, sanitize, constructParameters };
